# Minimal third-party requirements for the Documentation History / Cross-Reference analyzers
# Install with: pip install -r docs_analysis_requirements.txt

# Data processing
pandas>=1.3.0

# Visualization
matplotlib>=3.5.0

# Git interaction (optional, for more advanced git operations)
GitPython>=3.1.0

# HTTP requests used by cross-reference analyzer
requests>=2.28.0

# Notes:
# - The analyzers import many Python standard-library modules (argparse, pathlib, datetime,
#   collections, re, csv, json, os, subprocess, difflib, time). Those should NOT be listed
#   here — only third-party packages belong in this file.
Providing 4 ready-to-use prompts (each asks an agent to generate a Mermaid diagram) covering different viewpoints of your feature flags.

Feature dependency graph (high-level) Generate a Mermaid flowchart showing all major Cargo feature flags in this workspace and their relationships. Include nodes for: silero, vosk, text-injection, regex, level3, examples, gui, atspi, wl_clipboard, ydotool, kdotool, enigo. Show which features are default, which are optional, and any implicit runtime coupling (e.g., text-injection depends on at least one injector feature: atspi | wl_clipboard | enigo | (ydotool+kdotool fallback)). Use distinct styling: green = default-enabled, yellow = optional recommended, gray = legacy/experimental (level3). Add a legend subgraph. Output only valid Mermaid.

Crate ↔ feature map (structural view) Create a Mermaid graph mapping crates to the feature flags that affect their compilation paths. Left column: crates (coldvox-app, coldvox-audio, coldvox-vad, coldvox-vad-silero, coldvox-stt, coldvox-stt-vosk, coldvox-text-injection, coldvox-gui). Right column: features (silero, vosk, level3, text-injection, atspi, wl_clipboard, ydotool, kdotool, enigo, regex, gui, examples). Draw edges where a feature gates code in that crate. Cluster injector-related features together in a subgraph. Highlight transitive enabling (e.g., vosk implies STT runtime path in app). Use different edge styles: solid = direct cfg gating, dashed = runtime dependency (e.g., environment variable VOSK_MODEL_PATH). Output Mermaid only.

Build decision flow (environment + features) Produce a Mermaid flowchart representing build/runtime decision logic: start → read enabled Cargo features → detect platform (Wayland vs X11 vs other) → decide which injectors are constructed (atspi? wl_clipboard? ydotool? kdotool? enigo?) → if vosk enabled & model path exists → enable STT; else fallback (logging warning). Include branches for: text-injection disabled path, regex feature off path (string contains vs compiled regex). Emphasize termination states: “Full pipeline (VAD+STT+Injection)”, “Injection only”, “Audio only”. Output Mermaid only.

Injector availability layering (priority model) Create a Mermaid state or layered diagram showing injection method resolution order layers: Layer 1: Candidate methods derived from backend detection (Wayland → AtspiInsert, ClipboardAndPaste, Clipboard; X11 similar). Layer 2: Filter by feature availability (atspi, wl_clipboard, ydotool, kdotool, enigo). Layer 3: Filter by config flags (allow_ydotool, allow_kdotool, allow_enigo). Layer 4: Historical success-rate reordering. Layer 5: Fallback append NoOp. Represent each layer as a box transforming the set; annotate each pruning condition. Output valid Mermaid (flowchart or sequence-like transformation). Include note that combo ClipboardAndPaste requires (wl_clipboard && ydotool). Output Mermaid only.
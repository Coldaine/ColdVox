#!/usr/bin/env bash
set -uo pipefail

repo_root="$({
  cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd
})"

if ! "$repo_root/scripts/ensure_agent_hardlinks.sh" --quiet 2>/dev/null; then
  echo "warning: agent hardlink check failed (non-blocking)" >&2
fi

[package]
name = "coldvox-gui"
version = "0.1.0"
edition = "2021"
description = "GUI frontend for ColdVox (placeholder crate for future development)"

[[bin]]
name = "coldvox-gui"
path = "src/main.rs"

[dependencies]
# Using `eframe` (egui framework) for the initial GUI implementation.
eframe = "0.32"
